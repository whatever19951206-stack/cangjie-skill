---
name: {{skill-slug}}
description: |
  {{何时调用 + 何时不调用 + 关键 trigger 信号, ≤300 字}}
source_book: 《{{BOOK_TITLE}}》 {{AUTHOR}}
source_chapter: {{章节 / 页码 / 时间戳}}
tags: [{{tag1}}, {{tag2}}]
related_skills: []    # 阶段 3 填充
machine_bundle: skill.yaml
---

<!--
  填写说明（生成最终 skill 时删除本注释块）：
  - 本文件是人类可读说明；机器路由与发布状态以同目录 skill.yaml 为准。
  - description: 基于 A2 段浓缩，≤300 字，必须含“何时调用 + 何时不调用 + 关键 trigger 信号”。
  - source_chapter: 书填章节/页码；视频填时间戳/分 P；播客填集数。
  - R / I / A1 / B 中涉及来源的主张，要在 evidence.json 中有对应 evidence_id 和 claim_type。
  - related_skills 与 skill.yaml.routing.sibling_priority 必须同步。
-->

# {{Skill Title}}

## R — 原文 (Reading)

> {{原文引用，≤150 字（英文 ≤100 词），必须标注章节 / 页码 / 时间戳}}
>
> — {{AUTHOR}}, {{CHAPTER}}

- **证据 ID**: `{{ev_direct_quote_001}}`
- **claim_type**: `direct_quote`

---

## I — 方法论骨架 (Interpretation)

{{用自己的话重写，5–15 行。一个没读过原内容的人也应理解方法在做什么。禁止照搬原文，禁止把模型推断写成作者明确主张。}}

- **支持证据**: `{{ev_author_paraphrase_001}}`
- **claim_type**: `author_paraphrase` 或 `inferred_method`

---

## A1 — 原内容中的应用 (Past Application)

### 案例 1: {{案例名}}
- **问题**: {{作者遇到了什么}}
- **方法论的使用**: {{作者如何使用}}
- **结论**: {{得出了什么}}
- **结果**: {{实际发生了什么}}
- **证据 ID**: `{{ev_case_001}}`

### 案例 2（如有）

{{...}}

---

## A2 — 触发场景 (Future Trigger) ★

### 用户会在什么情境下需要这个 skill?

1. {{场景 1——具体到可识别的情况}}
2. {{场景 2}}
3. {{场景 3}}

### 语言信号（用户的话里出现这些可能激活）

- "{{典型措辞 1}}"
- "{{典型措辞 2}}"
- "{{典型措辞 3}}"

### 与相邻 skill 的区分

- 与 `{{related-skill-a}}` 的区别: {{...}}
- 与 `{{related-skill-b}}` 的区别: {{...}}

> 将以上内容同步到 `skill.yaml.routing.include / exclude / signals / sibling_priority`。

---

## E — 可执行步骤 (Execution)

当 skill 被激活后，agent 应按以下步骤执行：

1. **{{步骤 1}}**
   - 完成标准: {{如何判断这一步已完成}}

2. **{{步骤 2}}**
   - 完成标准: {{...}}
   - 判停条件: 若 {{X}} 则停止或跳到步骤 {{N}}

3. **{{步骤 3}}**
   - 完成标准: {{...}}

> 将步骤、完成标准和判停条件同步到 `skill.yaml.workflow`。

---

## B — 边界 (Boundary) ★

### 不要在以下情况使用此 skill

- {{反场景 1——为什么不适用}}
- {{反场景 2}}

### 作者在原内容中警告的失败模式

- {{来自 counter-example-extractor 的对应反例}}
- **证据 ID**: `{{ev_counterexample_001}}`

### 作者的盲点 / 时代局限

- {{来自阶段 0 批判阶段}}
- **claim_type**: `model_critique`（除非原文明确自我批判）

### 容易混淆的邻近方法论

- {{...}}

---

## 相关 skills（阶段 3 填充）

- depends-on: {{}}
- contrasts-with: {{}}
- composes-with: {{}}

---

## 审计信息

- **验证状态**: V1 {{✓/✗}} / V2 {{✓/✗}} / V3 {{✓/✗}}
- **机器状态**: `{{draft / tested / published}}`
- **hard_gate_passed**: `{{false / true}}`
- **测试通过率**: {{%}}（详见 `test-results.json`，不是只看测试题）
- **证据文件**: `evidence.json`
- **机器合同**: `skill.yaml`
- **蒸馏时间**: {{DATE}}
